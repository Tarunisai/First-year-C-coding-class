#include<stdio.h>
int main()
{
    char s[40];
    int i=1,length=0,flag=1;
    gets(s);
    for(i=0;s[i]!='\0';i++)
    {
        length++;
    }
   for(i=0;i<length/2;i++)
    {
        if(s[i]!=s[length-1-i])
        {
            flag=0;
            break;
        }
    }
    if(flag==1)
    printf("palindrome");
    else
    printf("not palindrome");
    return 0;
}
Output:
madam
palindrome
apple
not palindrome
