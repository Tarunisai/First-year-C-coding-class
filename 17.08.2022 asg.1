#include <stdio.h>
int main()
{   
   int i,n,sum=0,m;
	 scanf("%d",&n);
	  int arr[n-1];
	 for(i=0;i<n-1;i++)
	 {
	     scanf("%d",&arr[i]);
	     sum=sum+arr[i];
	 }
	 m=(n*(n+1))/2-sum;
	 printf("%d", m);
	 return 0;
}
Output:
5 
1 2 4 5  
3
10
1 2 3 4 5 6 8 9 10
7
