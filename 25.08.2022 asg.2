#include <stdio.h>
int main()
{
    char str[50];
    int i,a;
    printf("Enter a string");
    gets(str);
    for(i=0;str[i]!='\0';i++)
    {
     if(i%2==1)
    str[i]=str[i]-32;
    }
    puts(str);
    return 0;
}
Output:
Enter a string
aditya
aDiTyA
