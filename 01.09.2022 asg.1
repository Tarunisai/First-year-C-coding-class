#include<stdio.h>
int checkevenodd(int);
int main()
{
    int x,a;
    printf("Enter the number:");
    scanf("%d",&x);
    a=checkevenodd(x);
    if(a==0)
    printf("even");
    else
    printf("odd");
}
int checkevenodd(int x)
{
    if(x%2==0)
    return 0;
    else
    return 1;
}
Output:
Enter the number:6
even
