#include <stdio.h>
int main()
{
    char str[50];
    int i,count =0;
    printf("Enter a string:");
    gets(str);
    for(i=0;str[i]!='\0';i++)
    {
    if(str[i]==' ')
    count++;
    }
    printf("no. of words= %d",count);
    return 0;
}
Output:Enter a string: I love India
no.of words= 3
